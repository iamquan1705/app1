<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iamquan.qlnhansu">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QLNhanSu">
        <activity android:name=".activity.NhanvienmainActivity"></activity>
        <activity android:name=".activity.ThongtinnvActivity" />
        <activity android:name=".activity.AddnhanvienActivity" />
        <activity android:name=".activity.NhanvienbpActivity" />
        <activity android:name=".activity.EditbophanActivity" />
        <activity android:name=".activity.AddbophanActivity" />
        <activity android:name=".activity.BophanActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>